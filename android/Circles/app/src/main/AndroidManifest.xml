<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="perfectpi.circles"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="perfectpi.circles.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="perfectpi.circles.PiInfo"
            android:label="@string/title_activity_calculating_pi"
            android:parentActivityName="perfectpi.circles.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="perfectpi.circles.MainActivity" />
        </activity>

        <activity
            android:name="perfectpi.circles.AppInfo"
            android:label="@string/app_info_title"
            android:parentActivityName="perfectpi.circles.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="perfectpi.circles.MainActivity" />
        </activity>

        <activity
            android:name="perfectpi.circles.CalculatingPi"
            android:label="@string/title_activity_calculating_pi"
            android:parentActivityName="perfectpi.circles.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="perfectpi.circles.MainActivity" />
        </activity>

    </application>

</manifest>
